<div class="col-md-12">
    <div class="pull-right">
        <%= link_to("Edit", edit_job_path(@job), class: "btn btn-default") %>
    </div>
    <h1><%= @job.title %></h1>
    <p><%= @job.description %></p>
</div>
